import { StrictMode,Suspense,lazy } from 'react';import {createRoot} from 'react-dom/client';import {createBrowserRouter,RouterProvider} from 'react-router-dom';import {ThemeProvider} from './context/ThemeContext.jsx';import './styles/global.css';const Landing=lazy(()=>import('./pages/Landing.jsx'));const Viewer=lazy(()=>import('./pages/Viewer.jsx'));const NotFound=()=> <div style={{minHeight:'100vh',display:'grid',placeItems:'center',fontFamily:'system-ui'}}>Page not found.</div>;const router=createBrowserRouter([{path:'/',element:<Landing/>},{path:'/q/:token',element:<Viewer/>},{path:'*',element:<NotFound/>}]);createRoot(document.getElementById('root')).render(<StrictMode><ThemeProvider><Suspense fallback={<div style={{minHeight:'100vh',display:'grid',placeItems:'center'}}><span className="spinner"/></div>}><RouterProvider router={router}/></Suspense></ThemeProvider></StrictMode>);
